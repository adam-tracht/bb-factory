# Repository work dashboard

This table is the sole canonical dashboard for durable plan state. Each row links to its implementation source or canonical acceptance tracker. Status values are `Active`, `Blocked`, `Deferred`, `Needs Review`, and `Done`.
Implementation evidence is separate from rollout, cutover, production acceptance, and optional enhancement work. Source or Git evidence does not prove deployment.

## Factory

Unattended factory runs work on the `factory` branch and take tasks only from `plans/factory/queue.md`. Each run records results in `plans/factory/current.md` and `plans/factory/runs/`, and files open questions in `plans/factory/questions.md`. The run protocol is in `plans/factory/foreman.md`; repo-specific checks and limits are in `plans/factory/repo.md`.

Completed entries live in [`plans/factory/done.md`](./factory/done.md); older dashboard links to completed queue entries refer to that file.

## Tracked implementation and review

| ID | Work item | Status | Next action or remaining outcome | Evidence and canonical detail |
|---|---|---|---|---|
| BBF-0001 | [Spawn contract and registry schema](./factory/queue.md#bbf-0001-spawn-contract-and-registry-schema) | Done | None. | `environmentId` is optional across registry, add-input, and dispatch; [`spawnEnvironment`](../src/dispatch/types.ts) reuses a pinned env or spawns an unmanaged host workspace and records the auto-registered env id; storage CHECK relaxed via rebuild migration; 202 tests, typecheck, and lint pass. |
| BBF-0002 | [Protocol scaffolder](./factory/queue.md#bbf-0002-protocol-scaffolder) | Done | None. | [`templates/`](../templates/) ships the generic protocol set with a sha256 manifest tripwire; the `scaffold-protocol` action ([`src/actions/scaffold.ts`](../src/actions/scaffold.ts)) writes only missing files under create-only CAS, commits on `factory` via a host terminal, and refuses when the checkout is not on `factory`; 215 tests, typecheck, and lint pass. |
| BBF-0003 | [Dedicated checkout provisioning](./factory/queue.md#bbf-0003-dedicated-checkout-provisioning) | Done | None. | The `provision-checkout` action ([`src/actions/provision.ts`](../src/actions/provision.ts)) creates `<root>-factory` on `factory` via host terminals (shared runner in [`host-command.ts`](../src/actions/host-command.ts)), detects branch-in-use and already-provisioned idempotently, and offers a read-only direct-checkout mode; 240 tests, typecheck, and lint pass. |
| BBF-0004 | [Quickstart add-repository flow](./factory/queue.md#bbf-0004-quickstart-add-repository-flow) | Done | None. | Three-stage wizard (native folder pick, derived values, confirmed orchestration) over new `factory_pick_folder`, `factory_probe_repository`, and `factory_resolve_project` routes ([`repository-quickstart.ts`](../src/services/repository-quickstart.ts)); orchestrates provision, project resolution, paused registration, then scaffold with resume on partial failure. Verified live on the bb app: folder pick, derived review, branch-in-use offer, and paused registration all exercised against the real host; two live-host bugs were found and fixed during acceptance (`cd0adde` FK-paused migrations, `bced819` file-redirected command output). 276 tests, typecheck, lint, and build pass. |
| BBF-0005 | [Registry-first discovery gating](./factory/queue.md#bbf-0005-registry-first-discovery-gating) | Done | None. | Legacy flat settings carry `(legacy)` labels steering new installs to Add repository; the fresh-install status message points at the wizard; merge-state.sh dependency discovery was already gated (registry lookup preferred, missing file degrades quietly) and now has regression coverage. |
| BBF-0006 | [Release hygiene](./factory/queue.md#bbf-0006-release-hygiene) | Done | Marketplace submission under BBF-0007 approval. | package.json is publishable (license, repository, trimmed files); [`scripts/release.sh`](../scripts/release.sh) runs the full gate then builds the tagged release commit on `main` (tree minus `plans/`, plus `dist/`); CI runs typecheck, lint, test, SDK freshness, and build via `bb-app@0.42.1`; README rewritten for fresh installs. |
| BBF-0048 | [Native Tasks core rebuild](./factory/queue.md#bbf-0048-native-tasks-core-rebuild) | Active | Phase 0 feasibility spike, then adapter plus safety ledger, delegate-based dispatch, and queue cutover per the phased plan. | Plan and council decisions in [plans/native-tasks-migration.md](./native-tasks-migration.md); orchestrated from thread `thr_wswppd4ja4` on branch `factory-tasks`. |
| BBF-0007 | [Marketplace listing](./factory/queue.md#bbf-0007-marketplace-listing) | Active | Dependencies satisfied: repo is public at `adam-tracht/bb-factory`, `main` carries the `v0.1.3` release commit with `dist/` artifacts, tags `v0.1.0` through `v0.1.3` are pushed. The approved `v0.1.4` release is in preparation. Still needs an `approved:` line before the marketplace fork/PR is created. | Scope and acceptance are in [PLAN.md](../PLAN.md) Phase 6, deliverable 7. |
| BBF-0008 | Phase 1 rollout gate and hosting decision | Blocked | Verify always-on host, remote Connect owner-session route, and live run_detail path per docs/hosting-decision.md. | Human-gated, no queue entry; gate detail is in [docs/hosting-decision.md](../docs/hosting-decision.md) (the Phase 1 acceptance record was removed post-ship and lives in git history). |
| BBF-0009 | [Question deep links 404 on encoded hash](./factory/queue.md#bbf-0009-question-deep-links-404-on-encoded-hash) | Done | None. | Anchors now travel as a trailing `section/<anchor>` segment in [`src/ui/routes.ts`](../src/ui/routes.ts), which survives host percent-encoding; the parser decodes once and still rescues legacy `questions%23` and literal-`#` links. Regression coverage in [`tests/ui-routes.test.ts`](../tests/ui-routes.test.ts). |
| BBF-0010 | [Refresh glyph renders rotated](./factory/queue.md#bbf-0010-refresh-glyph-renders-rotated) | Done | None. | Raw U+21BB replaced by an inline SVG refresh icon (`RefreshIcon` in [`src/ui/shell.ts`](../src/ui/shell.ts)) stroked with `currentColor`; `tests/ui-shell.test.ts` asserts the svg element and the glyph's absence. |
| BBF-0011 | [Question-gated ready entries show "Ready" and Approve](./factory/queue.md#bbf-0011-question-gated-ready-entries-show-ready-and-approve) | Done | None. | [`src/ui/views/work.ts`](../src/ui/views/work.ts) folds open-question gating into the row badge (blocked-by warning treatment) and prefers the Answer CTA over Approve; the detail composer hides while gated; the stale `ui.test.ts` approval fixture was repointed to a non-gated draft entry. |
| BBF-0012 | [Approve on an already-ready entry is a dead end](./factory/queue.md#bbf-0012-approve-on-an-already-ready-entry-is-a-dead-end) | Done | None. | `approve-queue` now attaches the `approved:` line to a `status: ready` entry that lacks one ([`src/actions/repository.ts`](../src/actions/repository.ts)) instead of conflicting; the open-question gate still rejects, matching the UI's Answer CTA. Idempotent re-approval on matching text and conflict on different text are unchanged. |
| BBF-0013 | [First-class draft queue status](./factory/queue.md#bbf-0013-first-class-draft-queue-status) | Done | None. | `status: draft` parses to its own kind in [`markdown.ts`](../src/protocol/markdown.ts); draft entries render a neutral Draft badge in a collapsed Drafts group in [`work.ts`](../src/ui/views/work.ts), are never eligible, and no longer trip the unknown-status attention item. `templates/queue.md` and `MANIFEST.json` updated. |
| BBF-0014 | [Blocked-by badge drops the question id when a detail exists](./factory/queue.md#bbf-0014-blocked-by-badge-drops-the-question-id-when-a-detail-exists) | Done | None. | [`queueStatusLabel`](../src/ui/primitives.ts) renders `Blocked by Q<n>` so the question id is never dropped; the detail renders as a muted line under the row (mobile pass 2026-09-13). |
| BBF-0015 | [Stale question gates strand blocked entries](./factory/queue.md#bbf-0015-stale-question-gates-strand-blocked-entries) | Done | None. | [`reader.ts`](../src/protocol/reader.ts) emits `stale-question-gate` and `staleBlockingQuestionIds` when a `blocked-by` question is answered or missing; [work.ts](../src/ui/views/work.ts) puts the entry in Needs you with a Review CTA; [attention.ts](../src/ui/attention.ts) raises an action item; [foreman.md](../templates/foreman.md) requires re-filing leftover human work as a new question. Data fix shipped in diggs-data-platform `acc6097`. |
| BBF-0016 | ["All" repository switcher pill never shows active](./factory/queue.md#bbf-0016-all-repository-switcher-pill-never-shows-active-state) | Done | None. | Switcher takes a `repositoriesActive` signal from FactoryView so the All pill owns active state on the landing and no repo pill stays lit. |
| BBF-0017 | [Selecting a repository leaves the route on the landing view](./factory/queue.md#bbf-0017-selecting-a-repository-leaves-the-route-on-the-landing-view) | Done | None. | Landing `onSelect` now selects then navigates to overview, matching `ctx.onOpenRepository`. |
| BBF-0018 | [Run detail cannot open the worker thread](./factory/queue.md#bbf-0018-run-detail-cannot-open-the-worker-thread) | Done | None. | `RunDetailView` header renders an Open thread button for `workerThreadId`; attempt rows link their own thread ids. |
| BBF-0019 | [Settings Repository card is all read-only fields](./factory/queue.md#bbf-0019-settings-repository-card-is-all-read-only-fields) | Done | None. | Repository card keeps host status and the dispatch toggle inline; the eight read-only identity fields moved into a collapsed Repository details disclosure. |
| BBF-0020 | [Repository selection is dead and the panel thrashes](./factory/queue.md#bbf-0020-repository-selection-is-dead-and-the-panel-thrashes) | Done | None. | Pill/select clicks on landing-scoped routes now navigate to overview; reloads keep the switcher mounted with a busy dim instead of unmounting it; the selection override is keyed to the configured key so registry writes no longer snap back to the default repo; realtime events naming another repository no longer reload the visible one. |
| BBF-0021 | [Repo chrome stays rendered over the repositories landing](./factory/queue.md#bbf-0021-repo-chrome-stays-rendered-over-the-repositories-landing) | Done | None. | [`FactoryShell`](../src/ui/shell.ts) gates all repo-scoped chrome (branch/commit chip, dispatch chip, running/idle badge, Pause/Resume, Run now and its dialog, and the section tab bar) on `repositoriesActive`; the landing keeps only the title, switcher, refreshed indicator, legend, and banners. |
| BBF-0022 | [Plugin SDK pin drifted from host](./factory/queue.md#bbf-0022-plugin-sdk-pin-drifted-from-host-0447-vs-0484) | Done | None. | `@get-bb/plugin-sdk` repinned 0.4.47 to 0.4.84 to match the host; `HostInfo` in [`live-health.ts`](../src/services/live-health.ts) widened to the hosts.list element (the get result now adds `connectMachineId`) and the thread test fixture gained the three new nullable environment fields. `bb plugin types --check` passes again. |
| BBF-0023 | [Provider preference accepts any reported provider](./factory/queue.md#bbf-0023-provider-preference-accepts-any-reported-provider) | Done | None. | `providerPreference` is `alternate` or any providerId ([`contracts.ts`](../src/contracts.ts)); [`selectProvider`](../src/dispatch/preflight.ts) rotates and falls back across every usable reported provider in catalog order and gates on reported `full` permission support; the Settings dropdown lists all reported providers and the descriptor is a validated string; repo.md covers non-codex/claude-code foremen; 320 tests, typecheck, lint, and build pass. |
| BBF-0024 | [Multi-id question headings fail the whole protocol read](./factory/queue.md#bbf-0024-multi-id-question-headings-fail-the-whole-protocol-read) | Done | None. | The `parseQuestions` malformed-heading error names the expected shape and the single-id rule; `templates/questions.md` and `templates/foreman.md` document the one-token dashboard id and the lead-id-plus-`blocked-by` pattern for multi-gate questions; `MANIFEST.json` rehashed. Data fixes shipped in diggs-data-platform-factory `8da79bc` and monorepo-factory `1b21e2e`. |
| BBF-0025 | [Approve composer drafts scope and explains the ask](./factory/queue.md#bbf-0025-approve-composer-drafts-scope-and-explains-the-ask) | Done | None. | New advisory `recommend-approval` action spawns a thread that drafts an `approved:` line from the entry's plan, risk, and acceptance ([`contracts.ts`](../src/contracts.ts), [`interactions.ts`](../src/actions/interactions.ts)); the Approve composer in [`work.ts`](../src/ui/views/work.ts) now explains the gated actions, offers "Draft with agent" via a shared [`providerPicker.ts`](../src/ui/providerPicker.ts), and has a one-click "Routine scope only" preset. Storage v5 migration admits the new kind and normalizes absent `expectedRevision`. 328 tests, all gates pass. |
| BBF-0026 | [Run detail header declutter and visible thread button](./factory/queue.md#bbf-0026-run-detail-header-declutter-and-visible-thread-button) | Done | None. | [`RunDetailView`](../src/ui/views/runs.ts) drops the copyable run id from the header (still in Technical details) and renders Open thread as a bordered secondary button pinned to the header's right edge; regression coverage in [`tests/ui-runs.test.ts`](../tests/ui-runs.test.ts) asserts the id only renders inside a details element and the button carries the bordered styling. |
| BBF-0027 | [Remaining labels keep pill radii on phone widths](./factory/queue.md#bbf-0027-remaining-labels-keep-pill-radii-on-phone-widths) | Done | None. | Every text-bearing chip left on `rounded-full` (StateChip, section counts, tab count badges, answered/recorded chips) moved to `rounded-md`, matching the Badge treatment from the phone-width pass; only geometric circles (StatusDot, help icon) stay `rounded-full`. Pinned in [`tests/ui-shell.test.ts`](../tests/ui-shell.test.ts). |
| BBF-0028 | [Labels still read as round on small screens](./factory/queue.md#bbf-0028-labels-still-read-as-round-on-small-screens) | Done | None. | All text labels moved from `rounded-md` to bare `rounded`, the radius the mono id chips already used; rounded-md still looked round on small screens and tall labels. Controls and cards keep their radii; only StatusDot and the help icon stay circular. |
| BBF-0029 | [Run detail has no way back to the Runs list](./factory/queue.md#bbf-0029-run-detail-has-no-way-back-to-the-runs-list) | Done | None. | [`RunDetailView`](../src/ui/views/runs.ts) renders a ‹ Runs back control routing to the repository's Runs tab, which stays highlighted on detail routes, at a 44px tap target; browser back returns to the list. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0030 | [Phone header is crowded and the tab strip hides overflow](./factory/queue.md#bbf-0030-phone-header-is-crowded-and-the-tab-strip-hides-overflow) | Done | None. | [`shell.ts`](../src/ui/shell.ts) drops the `@sha` chip and copy button on phone widths, scrolls the active tab into view as the strip's overflow affordance, and grows small controls to 24px hit areas. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0031 | [Add-repository wizard keeps stale repository chrome](./factory/queue.md#bbf-0031-add-repository-wizard-keeps-stale-repository-chrome) | Done | None. | The `/repositories/new` wizard suppresses stale repository chrome, restores the originating view on Back, and uses host-specific folder-picker copy. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0032 | ["All" view has no aggregate Work, Questions, or Runs tabs](./factory/queue.md#bbf-0032-all-view-has-no-aggregate-work-questions-or-runs-tabs) | Done | None. | Aggregate Overview, Work, Questions, and Runs tabs compose existing per-repository projections, preserve repository-scoped actions, and handle partial settlement with collision-safe detail identity. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0033 | [Questions and Overview text overflows or hard-truncates](./factory/queue.md#bbf-0033-questions-and-overview-text-overflows-or-hard-truncates) | Done | None. | Question titles and Overview attention content now wrap or clamp without hiding required chips or ids. Responsive acceptance passed at 390px, 360px, 320px, and 1440px in light and dark themes with no page-level or element-level overflow. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, and whitespace. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0034 | [Work rows show contradictory chips and raw provenance](./factory/queue.md#bbf-0034-work-rows-show-contradictory-chips-and-raw-provenance) | Done | None. | Work rows now reflect effective eligibility, show quiet empty groups, and render consistent run, thread, and worker-finished provenance. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0035 | [Runs history rows misalign task chips on phone and mix status casing](./factory/queue.md#bbf-0035-runs-history-rows-misalign-task-chips-on-phone-and-mix-status-casing) | Done | None. | Runs history rows now separate metadata from wrapping task chips on phones, use consistent status casing, and show `No tasks` when applicable. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0036 | [Chip contrast fails AA and the legend and refresh cues are incomplete](./factory/queue.md#bbf-0036-chip-contrast-fails-aa-and-the-legend-and-refresh-cues-are-incomplete) | Done | None. | Chip contrast ratios measure 6.492 and 6.153, the legend covers all chip families, and refresh changes announce an `Updated` cue. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0037 | [Settings gives no save feedback and mislabels the dispatch toggle](./factory/queue.md#bbf-0037-settings-gives-no-save-feedback-and-mislabels-the-dispatch-toggle) | Done | None. | Settings now reports per-field Saving and Saved states, uses labeled dispatch controls, keeps helper copy truthful, and describes schedules as sentences. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0038 | [Info-dense tabs need progressive disclosure](./factory/queue.md#bbf-0038-info-dense-tabs-need-progressive-disclosure) | Done | None. | Overview, Work, Questions, and run detail now use repository-scoped persisted disclosure with phone-first defaults, count badges, and bounded deep-link reveal behavior. Final validation passed with 437 tests, typecheck, lint, build, SDK freshness, whitespace, and responsive visual acceptance. Commit [`4217a73`](https://github.com/adam-tracht/bb-factory/commit/4217a73). |
| BBF-0048 | [Repository display names](./factory/queue.md#bbf-0048-repository-display-names) | Done | None. Code-quality review and live visual validation explicitly deferred by the user. | Compliance corrections and automated validation complete in the uncommitted, unpushed working tree; 38 focused correction tests, 473 full-suite tests, typecheck, lint, build, SDK declarations, and whitespace validation pass. Registry-only alias, raw keys preserved for identity. |
| BBF-0040 | [Overview attention duplication and work-queue row layout](./factory/queue.md#bbf-0040-overview-attention-duplication-and-work-queue-row-layout) | Done | None. | Needs attention rows reveal detail only on expand so it never renders twice; desktop Work queue repository rows carry status chips and Open work inline in the header; repository subgroup headings render at normal weight. 465 tests, typecheck, and lint pass. |
| BBF-0041 | [Overview attention rows stack awkwardly on phone](./factory/queue.md#bbf-0041-overview-attention-rows-stack-awkwardly-on-phone) | Done | None. | Attention rows keep dot, title, and action on one line when collapsed; the expanded detail indents under the title instead of starting at the row edge. 473 tests, typecheck, and lint pass. |
| BBF-0042 | [Attention row severity dot drifts off the title line when expanded](./factory/queue.md#bbf-0042-attention-row-severity-dot-drifts-off-the-title-line-when-expanded) | Done | None. | The severity dot, caret, and row action all anchor to the title's first line at every width, so expanded rows no longer float the dot at mid-block. 473 tests, typecheck, and lint pass. |
| BBF-0043 | [Pick provider, model, and thinking on manual Run now](./factory/queue.md#bbf-0043-pick-provider-model-and-thinking-on-manual-run-now) | Done | None. | The run-now dialog offers Automatic (default, configured preference or rotation unchanged) vs a custom pick via the shared ProviderModelPicker; an explicit pick widens the `run-now` action with an all-or-none providerId/model/reasoningLevel triple plus optional serviceTier, fails `provider-unavailable` when the pick is unusable, and marks the spawn's `executionInputSources` explicit so host defaults are not re-derived. 484 tests, typecheck, lint, build, SDK freshness, whitespace pass. |
| BBF-0044 | [Default model and thinking per provider](./factory/queue.md#bbf-0044-default-model-and-thinking-per-provider) | Done | None. | Optional `providerModelDefaults` map (JSON string through a settings descriptor) persists `{ model, reasoningLevel }` per provider id; selectProvider applies it post-usability for pinned and alternate picks and records `+ configured default`; Settings shows a provider-pinned picker (or text/select fallback) only when a provider is pinned. 507 tests, typecheck, lint, build, SDK freshness, whitespace pass. |
| BBF-0045 | [User-configured alternate rotation of up to 5 providers](./factory/queue.md#bbf-0045-user-configured-alternate-rotation-of-up-to-5-providers) | Done | None. | Optional ordered `providerRotation` list (2-5 unique ids, JSON-string persisted) drives the alternate path: selectProvider advances through the configured list from `lastStartProvider`, skips unusable or unreported members, and falls back to any usable provider outside the list. The Settings rotation editor shows under `alternate` with add/remove/reorder, availability, and per-provider default hints. 524 tests, typecheck, lint, build, SDK freshness, whitespace pass. |
| BBF-0046 | [Attention row severity dot still drifts below the title line](./factory/queue.md#bbf-0046-attention-row-severity-dot-still-drifts-below-the-title-line) | Done | None. | The dot's span wrapper is now display:flex so the inline-block dot no longer baseline-aligns inside the wrapper's line box; dot, caret, and title centers coincide at every width. Verified by live measurement. 524 tests, typecheck, and lint pass. |
| BBF-0047 | [Agent-drafted queue entries](./factory/queue.md#bbf-0047-agent-drafted-queue-entries) | Done | None. | Draft tasks controls on the Work tab and as the add-repository wizard's final step spawn an advisory thread (guarded `draft-tasks` action, recommend-style spawn) that appends only `status: draft` entries to `queue.md` and commits them; drafts surface in the collapsed Drafts group and approve through the existing human-only control; foreman may file `draft` follow-ups it notices and reports them. 543 tests, typecheck, lint, build, SDK freshness, whitespace pass. |
