# Factory run record: BBF-0049 claim recovery and reviewed-state record

date: 2026-09-22T16:17:23Z
tasks: BBF-0049 claim recovery, review closeout, factory record updates
branch: factory
commits: see the task commit carrying this record
push: origin/factory
workers: thr_7kdsz5c29z (implementation), thr_99xjm4xpux (cleanup), thr_iuewm62zff, thr_i26tqvyfs6, thr_pptmb2ndn6 (independent reviews), thr_3qmy8wr93c (records, misfired)

## Supersedes

This record supersedes `2026-09-22-161236-thr_3qmy8wr93c.md`, which is retained
unaltered as immutable history. That run reported `state: blocked` on a required
rebase onto `origin/main`. The report was wrong. `plans/factory/repo.md` requires
**skipping** `git rebase origin/main` while main is release-shaped, and main is:
its nine most recent commits are all `release:` dist artifacts sitting above the
known `chore:`/`docs:` v0.1.0 cleanup at the branch base. That run rebased anyway
and hit the exact conflict the override exists to prevent, then aborted. No
repository state was damaged; the abort returned `factory` to `fcf96fd`. Its
"Notes for the next foreman" should not be acted on. Q5 is closed as not a
blocker.

## Preflight

- `factory` worktree clean; no lock file present.
- `git fetch origin --prune` succeeded.
- `git log origin/main -20 --format=%s` is release-shaped, so the rebase was
  correctly skipped per the repo override.

## Claim recovery

BBF-0049 was held by `in-progress (thread thr_wswppd4ja4, 2026-09-21T19:12Z)`.
That thread died mid-review and never released its claim. Released to
`status: ready` with a `recovered:` note, per the claim-recovery rule in the
queue header. The entry is not done: Go-live and cutover remain unstarted and
still require an explicit `approved:` line.

## Work recorded

Implementation lives on branch `factory-tasks` in the worktree
`/Users/adamtracht/Desktop/Code/bb-factory-tasks`, not on `factory`.

- `2e5e7cc` self-attributing settlement. The settlement write carries
  `status: done` plus an attempt-scoped `factory-settled:<attemptId>` marker in
  the card description, so a `done` card alone never proves Factory caused it.
- `ce2bce7` four review findings. Fresh card re-read immediately before the
  mutation to stop lost updates; settlement markers excluded from the approval
  content revision; settlement acceptance widened to any attempt of the same run
  while never accepting another run's; tolerant marker matching.
- `8253791` marker grammar cleanup. One shared parser for matching and
  stripping; the token ends at the marker character set so adjacent punctuation
  cannot corrupt it; symmetric trailing whitespace; dead public accessor removed.
- `47c4b9f` documented the reserved `factory-settled:` description prefix.

Plan status in `plans/native-tasks-migration.md`: Phases 0, 1, 2 and 3 complete;
Go-live and cutover unstarted.

## Reviews

Three independent reviews, one per code commit, each PASS with no blocking
findings. The third confirmed the run-scoping boundary holds against four attack
sequences (different run same card, recycled task id, marker with no landed
mutation, worker forgery), that the parser is safe against catastrophic
backtracking, and that the four original attribution races remain closed.

## Validation at 47c4b9f

- `pnpm test`: 660 tests across 32 files, pass
- `pnpm typecheck`: pass
- `pnpm lint`: pass
- `pnpm build`: pass
- `bb plugin types --check .`: pass, SDK pin and host both 0.4.104
- `git diff --check`: pass

## Validation on `factory` at this record

Records-only change; no source touched. Re-ran the standard checks on the
branch anyway because `fcf96fd` was an unvalidated unpushed code commit.

- `pnpm test`: 602 tests across 29 files, pass
- `pnpm typecheck`: pass
- `pnpm lint`: pass
- `pnpm build`: pass
- `bb plugin types --check .`: FAIL, pin 0.4.87 against host 0.4.104. Pre-exists
  this run and is filed as Q6; the fix is a dependency change needing approval.
- `git diff --check`: pass

## Questions

- Q4 stays open as an assumption: Go-live and cutover remain unstarted pending
  an explicit human `approved:` line.
- Q5 closed as not a blocker; see Supersedes above.
- Q6 filed blocking: the SDK pin on `factory` is 0.4.87 against host 0.4.104,
  so a required check fails. Repinning needs an `approved:` line.

## Notes for the next foreman

- BBF-0049 is `ready`. Do not start Go-live or cutover without an `approved:`
  line on the entry.
- `factory-tasks` is not merged into `factory`. It carries the Phase 0 to 3
  implementation.
- Do not rebase `factory` onto `origin/main` while main is release-shaped.

state: success
