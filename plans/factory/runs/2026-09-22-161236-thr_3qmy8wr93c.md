# Factory run record: BBF-0049 claim recovery blocked by preflight rebase

date: 2026-09-22T16:12:36Z
tasks: BBF-0049 claim recovery and factory record updates
branch: factory
commits: none
push: none
workers: none (interactive session; two read-only convention checks)

## Preflight

- The `factory` worktree was clean and `plans/factory/lock` was absent.
- `git fetch origin` succeeded. `origin/main` contains non-release history, so
  the repo override required the normal `git rebase origin/main`.
- The rebase conflicted while replaying `edea29a` (`release: bump version to
  0.1.1`) in `README.md` and `package.json`. The rebase was aborted as required
  by `plans/factory/foreman.md`.
- No queue, dashboard, current-run, source, test, or migration-plan changes
  were applied. BBF-0049 remains claimed by the stale thread until the manual
  rebase blocker is resolved.

## Requested record context

- The stale claim belongs to dead thread `thr_wswppd4ja4`. The requested
  recovery would release it to `status: ready` with a `recovered:` note.
- The implementation reported on branch `factory-tasks` consists of four
  commits: `2e5e7cc` adds self-attributing settlement markers;
  `ce2bce7` adds the fresh-card re-read, same-run settlement acceptance,
  revision filtering, and tolerant marker matching; `8253791` centralizes
  marker parsing and cleanup; `47c4b9f` documents the reserved
  `factory-settled:` prefix.
- Three independent reviews were reported as PASS with no blocking findings.
  Final reported validation on `47c4b9f` was 660 tests across 32 files,
  typecheck, lint, build, `bb plugin types --check .` with SDK pin and host at
  0.4.104, and `git diff --check`.
- No push was performed by that implementation work.

## Questions

- Q4 records the assumption that Go-live and cutover remain unstarted pending
  an explicit human `approved:` line.
- Q5 records the blocking manual-rebase requirement caused by this preflight
  conflict.

## Notes for the next foreman

- Resolve the `factory` versus `origin/main` history conflict, then rerun the
  bookkeeping session. Do not resolve it as part of the records-only task.
- The intended queue, dashboard, and current-run updates remain unapplied.
- No files under `src/` or `tests/` were touched, and nothing was pushed.

state: blocked
